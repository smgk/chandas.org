{
  "name": "chandas-org",
  "version": "1.14.2",
  "private": true,
  "description": "Offline-first Kannada and Devanagari meter composer",
  "author": "Ganesh Krishna Shankarathota",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "node scripts/build-static.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check meter_analysis.js && node --check strong_template.js && node --check poem_store.js && node --check app.js && node --check documentation.js && node --check service-worker.js && node scripts/validate-static.js",
    "test:e2e": "playwright test",
    "serve": "node scripts/static-server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
