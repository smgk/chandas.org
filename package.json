{
  "name": "chandas-org",
  "version": "1.27.0",
  "private": true,
  "description": "Offline-first Kannada, Telugu, and Devanagari meter composer",
  "author": "Ganesh Krishna Shankarathota",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "node scripts/build-static.js",
    "audit:meters": "node scripts/audit-meter-sources.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check meter_analysis.js && node --check scansion.js && node --check shithila_dvitva.js && node --check strong_template.js && node --check poem_store.js && node --check analytics.js && node --check app.js && node --check documentation.js && node --check service-worker.js && node scripts/validate-static.js",
    "test:e2e": "playwright test",
    "serve": "node scripts/static-server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
